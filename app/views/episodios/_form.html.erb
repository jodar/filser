<%= form_for(@episodio) do |f| %>
  <% if @episodio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@episodio.errors.count, "error") %> prohibited this episodio from being saved:</h2>

      <ul>
      <% @episodio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
